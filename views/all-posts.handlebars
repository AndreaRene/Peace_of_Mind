{{#if logged_in}}
<div>
  <h2>You are signed in as {{session.user_name}}! </h2>
</div>
{{/if}}
</div>
<br>
{{#if posts.length}}
<div>
  {{#each posts as |post|}}
  {{> post-partials post}}
  {{/each}}
</div>
{{/if}}