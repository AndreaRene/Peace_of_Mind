<div>
    <h2>You are signed in as {{session.user_name}}! </h2>
</div>
</div>
<br>
{{#if posts.length}}
<div>
    {{#each posts as |post|}}
    {{> dashboard-partials post}}
    {{/each}}
</div>
{{/if}}