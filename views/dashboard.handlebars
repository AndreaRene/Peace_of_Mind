<div>
    <h2>You are signed in as {{session.user_name}}! </h2>
</div>
<a href="/api/dashboard/new">
    New Post
</a>
<br>
{{#if posts.length}}
<div>
    {{#each posts as |post|}}
    {{> dashboard-partials post}}
    {{/each}}
</div>
{{/if}}